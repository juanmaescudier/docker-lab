# requirements.txt del servicio "api"

Flask==3.1.3
gunicorn==23.0.0
Flask-SQLAlchemy==3.1.1
psycopg[binary]==3.2.9
Flask-Session==0.8.0
redis==5.2.1
prometheus-flask-exporter==0.23.2
# Migraciones de esquema. Se ejecuta a mano (alembic upgrade head), pero va en la
# imagen para poder lanzarlo dentro del contenedor y con las mismas versiones.
alembic==1.16.5
