# .env.example — plantilla de variables de entorno.
# ESTE archivo SÍ se sube al repo (con valores de mentira).
# El .env real (con valores reales) NO se sube (está en .gitignore).
# Lo relleno yo cuando definamos las variables del M0.
